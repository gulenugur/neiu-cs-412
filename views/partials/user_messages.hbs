<div class="row justify-content-center m-2">
    {{#if flashMessages}}
        {{#if flashMessages.success}}
            <span class="text-black-50">{{{flashMessages.success}}}</span>
        {{else}}
            <span class="text-warning">{{{flashMessages.error}}}</span>
        {{/if}}
    {{/if}}
</div>